{{- if .Values.serviceAccount.create }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "helm-templates-demo.serviceAccountName" . }}
  labels:
    {{- include "helm-templates-demo.labels" . | nindent 4 }}
{{- end }}
